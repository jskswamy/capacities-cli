node_modules/
dist/
coverage/
*.tmp
docs/superpowers/

# Beads / Dolt files (added by bd init) — synced via DoltHub, not git
.dolt/
*.db
.beads-credential-key
.beads/

# Local codebase-memory-mcp indexing state (machine-specific)
.claude/codebase.local.md
