node_modules/
dist/
coverage/
*.tmp
docs/superpowers/

# Beads / Dolt files (added by bd init) — synced via DoltHub, not git
.dolt/
*.db
.beads-credential-key
.beads/

# Local codebase-memory-mcp indexing state (machine-specific)
.claude/codebase.local.md

# Repo-local tool state for pre-commit hooks (semgrep/trivy) — keeps checks
# self-contained instead of touching each contributor's ~/.semgrep or ~/.docker
.tool-cache/
